{
  "enabledPlugins": {
    "python-library-complete@python-library-dev": true
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json; d=json.load(sys.stdin); print(d.get('tool_input',{}).get('file_path',''))\" | { read -r f; if echo \"$f\" | grep -qE '\\.py$'; then uv run mypy src tests; fi; }",
            "statusMessage": "Running mypy..."
          }
        ]
      }
    ]
  }
}
